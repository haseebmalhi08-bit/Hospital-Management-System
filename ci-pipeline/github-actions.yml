name: HMS Verification Pipeline

on:
  push:

jobs:

  validation:

    runs-on: ubuntu-latest

    steps:

      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Run Validation
        run: echo "Validation Successful"
